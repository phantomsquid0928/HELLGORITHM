#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    ios::sync_with_stdio(0);
    cout.tie(0);
    cin >>t;
    for (int i = 0; i< t; i++) {
        cout << i + 1 << '\n';
    }
}